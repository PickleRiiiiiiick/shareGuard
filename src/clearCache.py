# Import the GroupResolver class
from scanner.group_resolver import GroupResolver

# Instantiate the GroupResolver
resolver = GroupResolver()

# Clear the cache
resolver.clear_cache()

print("Cache has been successfully cleared.")
